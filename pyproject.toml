[project]
name = "mail-agent"
version = "0.1.0"
description = "CLI email agent + mutual-fund document requester (Gmail API OAuth2, SMTP, or himalaya backends)"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = ["python-dotenv>=1.2.2", "casparser>=0.8.0", "pyxirr>=0.10.8", "google-api-python-client>=2.184.0", "google-auth-oauthlib>=1.2.2", "google-auth>=2.42.0"]

[project.optional-dependencies]
dev = ["pytest>=8.4.2"]
microsoft = ["msgraph-sdk>=1.0.0", "azure-identity>=1.19.0"]

[project.scripts]
mail-agent = "gsuite_agent.request:main"
gtool = "gsuite_agent.gtool:main"
gsuite = "gsuite_agent.gtool:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gsuite_agent"]
artifacts = ["gsuite_agent/templates/*.txt", "gsuite_agent/templates/*.json"]

[tool.pytest.ini_options]
pythonpath = ["."]
